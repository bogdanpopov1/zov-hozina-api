# 🖥️ Как найти консоль на Railway

## Пошаговая инструкция

### 1. Зайдите в ваш проект
- Откройте [railway.app](https://railway.app)
- Войдите в аккаунт
- Выберите проект "valiant-reflection"

### 2. Найдите ваш веб-сервис
- В левой панели найдите сервис с названием "web" или "app"
- Нажмите на него

### 3. Перейдите в раздел "Deployments"
- В верхней части страницы найдите вкладки
- Нажмите на вкладку **"Deployments"**

### 4. Выберите активный деплоймент
- Найдите деплоймент со статусом "Active" или "Deployed"
- Нажмите на него

### 5. Найдите кнопку "Console" или "Terminal"
- Внутри деплоймента ищите кнопку с текстом:
  - "Console"
  - "Terminal" 
  - "Open Console"
  - "Shell"
- Обычно она находится в правом верхнем углу или рядом с логами

### 6. Альтернативные места поиска консоли

Если не нашли в Deployments, попробуйте:

#### Вариант A: Вкладка "Logs"
- Перейдите во вкладку "Logs"
- Ищите кнопку "Console" или "Terminal" в правом верхнем углу

#### Вариант B: Вкладка "Metrics"
- Иногда консоль может быть в разделе "Metrics"
- Ищите кнопку "Open Shell" или "Terminal"

#### Вариант C: Прямо в сервисе
- Некоторые версии Railway показывают кнопку "Console" прямо в главном меню сервиса
- Ищите иконку терминала или кнопку "Shell"

## 🔧 Что делать в консоли

После того как найдете консоль, выполните:

```bash
# Проверим, где находится PHP
which php
# или
find /usr -name php
# или
ls -la /usr/bin/ | grep php

# Если нашли PHP, выполните миграции
/usr/bin/php artisan migrate --force
/usr/bin/php artisan db:seed --force

# Если PHP не найден, попробуйте альтернативные пути
/usr/local/bin/php artisan migrate --force
/opt/php/bin/php artisan migrate --force
```

## 🚨 Если консоль не найдена

### Альтернативный способ через Railway CLI

1. **Обновите Railway CLI:**
   ```bash
   npm install -g @railway/cli@latest
   ```

2. **Попробуйте команду shell:**
   ```bash
   railway shell
   ```

3. **Или используйте наш скрипт:**
   ```bash
   railway run bash run-migrations-railway.sh
   ```

### Через переменные окружения

Добавьте переменную окружения на Railway:
- `RAILWAY_RUN_MIGRATIONS=true`
- Перезапустите сервис

## 📱 Скриншоты интерфейса Railway

Если интерфейс изменился, ищите:
- Иконку терминала 🖥️
- Кнопку "Console"
- Кнопку "Terminal" 
- Кнопку "Shell"
- Кнопку "Open Console"

Обычно они находятся в правом верхнем углу или рядом с логами деплоймента.
